import styled from "styled-components";

const selector = "div";

export const Row = styled.div`
  ${selector} a {
    display: block
  }
  a ${selector} {
    display: block
  }
  ${selector}
  a {
    display: block
  }
  a
  ${selector} {
    display: block
  }
  ${selector},
  a {
    display: block
  }
  a,
  ${selector} {
    display: block
  }
`;
